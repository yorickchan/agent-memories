{
    "name": "agentmemories",
    "version": "0.1.0",
    "author": "Yorick Chan <yorick@gmail.com> ",
    "type": "module",
    "private": true,
    "workspaces": [
        "packages/*"
    ],
    "scripts": {
        "agent-memories-mcp": "bun run --cwd packages/mcp src/cli.ts",
        "build": "bun run --filter '*' build",
        "test": "bun run --filter '*' test",
        "typecheck": "bun run --filter '*' typecheck"
    },
    "devDependencies": {
        "@types/node": "^26.1.0",
        "bun-types": "^1.3.14",
        "typescript": "^6.0.3",
        "vitest": "^4.1.9"
    }
}